<nav class="navbar is-transparent">
    <div class="navbar-brand">
        <a class="navbar-item" href="/">
            <img src="https://bulma.io/images/bulma-logo.png" alt="Bulma: a modern CSS framework based on Flexbox"
                 width="112" height="28">
        </a>
        <div class="navbar-burger burger" data-target="navbarExampleTransparentExample">
            <span></span>
            <span></span>
            <span></span>
        </div>
    </div>
    <div id="navbarExampleTransparentExample" class="navbar-menu">
        {% if header_menu is defined %}
            {% if header_menu|length > 0 %}
                    <div class="navbar-start">
                        <a class="navbar-item" href="/">
                            Главная
                        </a>
                        <div class="navbar-item has-dropdown is-hoverable">
                            <a class="navbar-link" href="/doc">
                                Документация
                            </a>
                            <div class="navbar-dropdown is-boxed">
                                {% for item in header_menu %}
                                    {% if loop.first %}
                                        <a class="navbar-item active" href="#">{{ item|e }}</a>
                                    {% else %}
                                        <a class="navbar-item" href="#">{{ item|e }}</a>
                                    {% endif %}
                                {% endfor %}
                            </div>
                        </div>
                    </div>
            {% endif %}
        {% endif %}
        <div class="navbar-end">
            <div class="navbar-item">
                <div class="field is-grouped">
                    <p class="control">
                        <a class="bd-tw-button button" data-social-network="VKontakte" data-social-action="vk"
                           data-social-target="http://localhost:4000" target="_blank"
                           href="https://vk.com/mc_sharingan">
                            <span class="icon">
                                <i class="fa fa-vk"></i>
                            </span>
                            <span>Tweet</span>
                        </a>
                    </p>
                    <p class="control">
                        <a class="button is-primary" href="/">
                          <span class="icon">
                            <i class="fa fa-download"></i>
                          </span>
                            <span>Регистрация</span>
                        </a>
                    </p>
                </div>
            </div>
        </div>
    </div>
</nav>